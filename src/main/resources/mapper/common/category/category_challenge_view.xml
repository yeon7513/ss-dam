<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="categoryChallengeView">

  <!-- 피드 목록용 챌린지 카테고리 조회 (진행중, 진행완료 포함) -->
  <select id="loadAllChallengeCategories" resultType="AdminChallengeCategoryView">
    SELECT
    c.code,
    c.title AS name,
    c.progress_status,
    c.post_status,
    c.created_by,
    c.created_at,
    c.updated_by,
    c.updated_at,
    c.delete_yn
    FROM CHALLENGE AS c
    WHERE c.delete_yn = 0
  </select>

  <!-- 피드 등록용 챌린지 카테고리 조회 (진행중인 것만) -->
  <select id="loadActiveChallengeCategories" resultType="UserChallengeCategoryView">
    SELECT c.code, c.title AS name, c.progress_status
    FROM CHALLENGE AS c
    WHERE c.progress_status = 'IN_PROGRESS'
    AND c.post_status = 'ACTIVE'
    AND c.delete_yn = 0
  </select>

</mapper>
