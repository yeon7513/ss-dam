<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="category">

  <!-- 피드 목록용 챌린지 카테고리 조회 (진행중, 진행완료 포함) -->
  <select id="loadAllChallengeCategories" resultType="Category">
    SELECT c.code, c.title AS name
    FROM CHALLENGE AS c
    WHERE c.delete_yn = 0
  </select>

  <!-- 피드 등록용 챌린지 카테고리 조회 (진행중인 것만) -->
  <select id="loadActiveChallengeCategories" resultType="Category">
    SELECT c.code, c.title AS name
    FROM CHALLENGE AS c
    WHERE c.progress_status = 'IN_PROGRESS' AND c.post_status = 'ACTIVE' AND c.delete_yn = 0
  </select>


  <!-- 물품 등록용 마켓 카테고리 선택 (상위 & 하위) -->
  <select id="loadActiveMarketCategories" resultType="Category">
    SELECT
    sub.code,
    sub.name,
    top.code AS top_category_code,
    top.name AS top_category_name
    FROM MARKET_CATEGORY AS sub
    LEFT JOIN MARKET_CATEGORY AS top
    ON sub.main_cate = top.code
    <!--
      [조건]
      상위 카테고리가 활성화 및 삭제되지 않고, 하위도 활성화 및 삭제되지 않은 것
    -->
    WHERE top.delete_yn = 0 AND top.status = 'ACTIVE'
    AND sub.delete_yn = 0 AND sub.status = 'ACTIVE'
  </select>

</mapper>
