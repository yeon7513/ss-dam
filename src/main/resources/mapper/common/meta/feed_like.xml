<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="feedLike">
	<!-- UPSERT -->
	<insert id="upsertFeedLike" parameterType="FeedLike">
		INSERT INTO FEED_LIKE(
		FEED_CODE,
		MEM_CODE,
		CREATED_AT,
		DELETE_YN
		)VALUES(
		{feedCode},
		{memCode},
		NOW(),
		'N'
		)
		ON DUPLICATE KEY UPDATE
		DELETE_YN = IF(DELETE_YN = 'N', 'Y', 'N')
	</insert>

	<select id="selectedIsLiked" parameterType="map"
		resultType="boolean">
		SELECT COUNT(*) > 0
		FROM FEED_LIKE
		WHERE FEED_CODE =
		#{feedCode}
		AND MEM_CODE = #{memCode}
		AND DELETE_YN = 'N'
	</select>

</mapper>