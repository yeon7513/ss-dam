<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="chatCommand">
  
  <!-- 신규 채팅방 저장 -->
  <insert id="registerChatRoom">
    INSERT INTO CHAT_ROOM (room_id, type, target_code, requester_code, responder_code, created_at)
    VALUES (#{roomId}, #{type}, #{targetCode}, #{requesterCode}, #{responderCode}, NOW())
  </insert>

  <!-- 신규 채팅 메시지 저장 -->
  <insert id="registerChatMessage">
    INSERT INTO CHAT_LOG (room_code, sender_code, message, created_at)
    VALUES (#{roomCode}, #{senderCode}, #{message}, NOW())
    <selectKey keyProperty="code" resultType="long" order="AFTER">
      SELECT LAST_INSERT_ID()
    </selectKey>
  </insert>

</mapper>
