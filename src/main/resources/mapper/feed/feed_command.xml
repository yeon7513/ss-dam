<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 이 매퍼는 insert, update용 -->
<mapper namespace="feedCommand">

  <!-- 피드 등록 -->
  <insert id="registerFeed">
    INSERT INTO FEED (chal_code, mem_code, title, content, hitcount, status, created_by, created_at, delete_yn)
    VALUES (#{chalCode}, #{memCode}, #{title}, #{content}, 0, 'ACTIVE', #{createdBy}, NOW(), 0)

    <selectKey keyProperty="code" resultType="long" order="AFTER">
      SELECT LAST_INSERT_ID()
    </selectKey>
  </insert>

  <!-- 해시태그 등록 -->
  <insert id="registerHashtags">
    INSERT INTO FEED_HASHTAG (feed_code, tag_name)
    VALUES
    <foreach collection="list" item="item" separator=",">
      (#{item.feedCode}, #{item.tagName})
    </foreach>
  </insert>

  <!-- 해시태그 삭제 -->
  <delete id="deleteHashtags">
    DELETE FROM FEED_HASHTAG
    WHERE feed_code = #{feedCode}
  </delete>

  <!-- 피드 수정 (피드 작성자) -->
  <update id="updateFeed">
    UPDATE FEED
    SET
    chal_code = #{chalCode},
    title = #{title},
    content = #{content},
    updated_by = #{updatedBy},
    updated_at = NOW(),
    delete_yn = #{deleteYn}
    WHERE code = #{code}
  </update>

  <!-- 피드 삭제 (피드 작성자) -->
  <update id="deleteFeed">
    UPDATE FEED
    SET
    delete_yn = 1,
    status = 'DELETED',
    updated_by = #{updatedBy},
    updated_at = NOW()
    WHERE code = #{feedCode}
  </update>

  <!-- 피드 수정 (관리자) -->

</mapper>

