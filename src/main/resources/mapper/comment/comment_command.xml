<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="commentCommand">

    <!-- 댓글 등록 -->
    <insert id="registerComment">
        INSERT INTO COMMENTS (
        feed_code,
        mem_code,
        content,
        status,
        created_by,
        created_at,
        delete_yn
        )
        VALUES (
        #{feedCode},
        #{memCode},
        #{content},
        'active',
        (
        SELECT id
        FROM MEMBER_ACCOUNT
        WHERE code = #{memCode}
        ),
        NOW(),
        0
        )

        <selectKey
                keyProperty="code"
                resultType="long"
                order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
    </insert>

      <!-- 댓글 수정 -->
  <update id="updateComment">
  UPDATE COMMENTS
  SET
    content = #{content},
    updated_at = NOW()
  WHERE code = #{commentCode}
    AND status = 'active'
    AND delete_yn = 0
    </update>
<!-- 현재는 로그인 연동 전이므로 updated_by는 수정X,나중에 세션을 적용하면 다음처럼 확장
    request.setMemberCode(loginUser.getCode());
    request.setUpdatedBy(loginUser.getMemberId());-->
</mapper>