<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="comment">

	<select id="countComment" resultType="Integer">
		SELECT IFNULL(count(*), 0)
		FROM comments
		WHERE feed_code = #{feedCode}
	</select>

	<select id="searchCommentsByFeedCode" resultType="Comment">
		SELECT ifnull(count(cl.cmt_code), 0) AS count_like, c.*, profile_img, ranking
		FROM comments c
		JOIN member_account ma
			ON c.member_id = ma.id
		LEFT JOIN comment_like cl
			ON cl.cmt_code = c.code
		WHERE feed_code = 3
		GROUP BY c.member_id
	</select>
</mapper>