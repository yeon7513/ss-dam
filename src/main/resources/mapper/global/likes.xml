<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="likes">

	<!-- 피드 좋아요 개수 -->
	<select id="countFeedLike" resultType="Integer">
		SELECT IFNULL(count(*), 0)
		FROM FEED_LIKE
		WHERE feed_code = #{feedCode}
	</select>
	
	<!-- 댓글 좋아요 개수 -->
	<select id="countCommetLike" resultType="Integer">
		SELECT IFNULL(count(*), 0)
		FROM COMMENT_LIKE
		WHERE cmt_code = #{cmtCode}
	</select>
	
</mapper>