<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="image">
	
	<select id="searchImagesByCode" resultType="Images">
		SELECT *
		FROM images
		WHERE type = #{type} AND target_code = #{code}
	</select>
	
	<insert id="registerImages">
		INSERT INTO images 
		(target_code, type, path, delete_yn)
		VALUES (#{targetCode}, #{type}, #{path}, #{deleteYn})
	</insert>

</mapper>
